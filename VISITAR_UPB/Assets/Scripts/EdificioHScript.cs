using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EdificioHScript : MonoBehaviour
{
    public GameObject Arrow;
    Quaternion haciaescaleras1 =  Quaternion.Euler(50,120,80);
    Quaternion haciaedificioj =  Quaternion.Euler(20,40,180);
    Quaternion haciaedificiod =  Quaternion.Euler(1,200,20);
    Quaternion haciaedificiog =  Quaternion.Euler(50,120,80);

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
