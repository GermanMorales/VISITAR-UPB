using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EdificioKScript : MonoBehaviour
{
    public GameObject Arrow;
    Quaternion haciaescaleras3 =  Quaternion.Euler(50,120,80);
    Quaternion haciaparqueadero =  Quaternion.Euler(20,40,180);
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
