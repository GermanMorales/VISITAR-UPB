using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EdificioIScript : MonoBehaviour
{
    public GameObject Arrow;
    Quaternion haciaescaleras3 =  Quaternion.Euler(50,120,80);
    Quaternion haciaescalerasfork =  Quaternion.Euler(20,40,180);
    Quaternion haciacarretera =  Quaternion.Euler(1,200,20);
    Quaternion haciaedificiol =  Quaternion.Euler(50,120,80);
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
