using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CarreteraScript : MonoBehaviour
{
    public GameObject Arrow;
    Quaternion haciaedificiog =  Quaternion.Euler(50,120,80);
    Quaternion haciaedificioi =  Quaternion.Euler(20,40,180);
    Quaternion haciaedificiol =  Quaternion.Euler(1,200,20);
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
