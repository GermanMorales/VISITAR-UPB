using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EdificioJScript : MonoBehaviour
{
    public GameObject Arrow;
    Quaternion haciaedificiog =  Quaternion.Euler(50,120,80);
    Quaternion haciaedificiod =  Quaternion.Euler(20,40,180);
    Quaternion haciaporteria =  Quaternion.Euler(1,200,20);
    Quaternion haciaedificioh =  Quaternion.Euler(50,120,80);
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
