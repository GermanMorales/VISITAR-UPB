using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FruteriaScript : MonoBehaviour
{
    public GameObject Arrow;
    Quaternion haciaedificioa =  Quaternion.Euler(50,120,80);
    Quaternion haciaedificiob =  Quaternion.Euler(20,40,180);
    Quaternion haciaedificioc =  Quaternion.Euler(1,200,20);
    Quaternion haciaedificiod =  Quaternion.Euler(50,120,80);
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
