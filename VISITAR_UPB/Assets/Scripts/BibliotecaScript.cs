using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BibliotecaScript : MonoBehaviour
{
    public GameObject Arrow;
    Quaternion haciaporteria =  Quaternion.Euler(50,120,80);
    Quaternion haciaedificioc =  Quaternion.Euler(20,40,180);
    Quaternion haciaescaleras1 =  Quaternion.Euler(1,200,20);

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
