using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EdificioFScript : MonoBehaviour
{
    public GameObject Arrow;
    Quaternion haciaplaza =  Quaternion.Euler(50,120,80);
    Quaternion haciaedificioe =  Quaternion.Euler(20,40,180);

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
