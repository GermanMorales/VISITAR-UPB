using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EscaleraforkScript : MonoBehaviour
{
    public GameObject Arrow;
    Quaternion haciaedificioe =  Quaternion.Euler(50,120,80);
    Quaternion haciaedificioi =  Quaternion.Euler(20,40,180);
    Quaternion haciaescaleras3 =  Quaternion.Euler(1,200,20);
    Quaternion haciaedificiod =  Quaternion.Euler(50,120,80);
    Quaternion haciaescaleras2 =  Quaternion.Euler(50,120,80);
   
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
