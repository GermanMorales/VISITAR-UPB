using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Escaleras3Script : MonoBehaviour
{
    public GameObject Arrow;
    Quaternion haciaedificiok =  Quaternion.Euler(50,120,80);
    Quaternion haciaedificioi =  Quaternion.Euler(20,40,180);
    Quaternion haciaescalerasfork =  Quaternion.Euler(1,200,20);
    Quaternion haciaedificiol =  Quaternion.Euler(50,120,80);
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
